<!DOCTYPE html>
<html lang="en">
<head>
    <title>Code test</title>
    <!-- <link rel="stylesheet" href="/style.css"> -->
    <style>
        * {
    margin: 0;
    padding: 0;
}

body {
    background-color: LightSalmon;
    font-family: sans-serif;
}

th {
    padding: 8px 40px 8px 0px;
}

td {
    color: #4A4A4A;
    padding: 8px 0;
}
th {
    color: #2b2b2b;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 1.2em;
}
tr {
    padding-bottom: 100px;
}

.vehicle-list {
    background-color: white;
    max-width: 50%;
    border-radius: 0.4rem;
    margin: 100px auto;
    padding: 2rem;
    box-shadow: Salmon 8px 8px 40px;
}

.form {
    display: flex;
    flex-direction: row;
}

.add-vehicle {
    margin-bottom: 2em;
    border-bottom: 1px #dddddd solid;
}

.heading-3 {
    margin-bottom: 12px;
    font-size: 1.2rem;
    text-transform: uppercase;
}
.input-form input, select {
    padding: 8px 16px;
    margin: 4px 0 16px 0;
    border: #4A4A4A 1px solid;
    border-radius: 4px;
    letter-spacing: 2px;
    /* text-transform: uppercase; */
}
.input-form {
    margin-right: 1em;
    display: inline-block;
}
label {
    color: #4A4A4A;
    font-weight: 600;
}
    </style>
</head>
<body>
    <div class="vehicle-list">
        <div class="add-vehicle">
            <h3 class="heading-3">Add a vehicle</h3>
            <div class="form">
                <form method="post" action="/" id='vehicle-form'>
                    <div class="input-form">
                        <label for="vtype">Type</label>
                        <br>
                        <input type="text" name="vtype" id="vtype" class='vtype'>
                    </div>
                    <div class="input-form">
                        <label for="vmilage">Milage</label>
                        <br>
                        <input type="text" name="vmilage" id="vmilage" class='vmilage'>
                    </div>
                    <div class="input-form">
                        <label for="vhybrid">Hybid?</label> <br>
                        <select name="vhybrid" id="vhybrid">
                            <option [ngValue]="true">Yes</option>
                            <option [ngValue]="false">No</option>
                        </select>
                    </div> 
                    <button type="submit" id="addVehicleBtn" onsubmit="addV">Click me</button>
                    <!-- <input type="submit" value="Second button" id="addVehicleBtn"> -->
                </form>
            </div>
        </div>
        

        <table>
            <tr>
                <th>Vehicle Type</th>
                <th>Milage</th>
                <th>Hybrid</th>
            </tr>

            <% if (vehicleRepo.length > 0) { %>
                <% vehicleRepo.forEach(v => { %>
                    <tr>
                        <td><%= v.type %></td>
                        <td><%= v.milage %></td>
                        <td><%= v.hybrid %></td>
                    </tr>
                <% }) %>
            <% } %>
            

        </table>
    </div>

    <script>

        let vehicleType = document.querySelector('.vtype');
        let vehicleMilage = document.querySelector('.vmilage');
        let vehicleHybrid = document.querySelector('#vhybrid').value;
        const vehicleButton = document.querySelector('#addVehicleBtn');
        const form = document.querySelector('#vehicle-form');

        const addV = (e) => {
            // e.preventDefault();
            // return;
        };

        getV = () => {
            console.log('from the backend')
        };
        getV();
        form.addEventListener("submit", addV);
        vehicleButton.addEventListener("click", addV);

    </script>
</body>
</html>
